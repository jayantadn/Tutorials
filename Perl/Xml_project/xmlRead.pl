use strict;
use Data::Dumper;
use XML::Simple;

#my @var = ( "Jayanta", "Mohit", "Mounika" );
#print Dumper( \@var );

#my %assarray = (
#    Name => "Jayanta",
#    Company => "Bosch"
#);

#print Dumper( \%assarray );

#my $booklist = XMLin( 'booklist.xml' );
#my $booklist = XMLin( 'booklist.xml', KeyAttr=>{ book=>'isbn' } );

#print $booklist->{book}->[0]->{isbn};
#print $booklist->{book}->{Book3ISBN}->{title};

#print Dumper( $booklist );

#my $var;
#foreach $var ( @{ $booklist->{book} } )
#{
#    print $var->{title} . "\n";
#}